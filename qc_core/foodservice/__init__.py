"""Foodservice utility schedule vs electrical kitchen equipment schedule check.

Both schedules are rotated 90 degrees on the sheet: equipment items run along
the x-axis (one column per item), attributes run along the y-axis (one band per
column header). See qc_core.foodservice.extract for the shared rotated-table
reader and the two schedule-specific extractors.
"""
